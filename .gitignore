node_modules
dist
*.log

# secrets — never commit the real key (keep .env.example)
.env
.env.local
.env.*.local

# OS / editor cruft
.DS_Store

# Vite temp files
vite.config.js.timestamp-*.mjs
vite.config.ts.timestamp-*.mjs

# local verification scratch
t.mjs
t.nc
